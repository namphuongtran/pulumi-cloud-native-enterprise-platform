name: foundation-state-backend
runtime: nodejs
description: |
  Foundation Layer: Pulumi State Backend
  Deploys Azure Blob Storage for storing Pulumi state (stack state, resources, outputs)
  
  This must be deployed FIRST before any other stacks.
  
  Stack naming: state-backend-{environment}-{location}
  Example: state-backend-prod-eastus
